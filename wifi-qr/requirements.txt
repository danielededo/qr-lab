# QR generation — pure Python, no external dependencies of its own.
segno>=1.6.6

# QR decoding — the headless build is enough (we never open GUI windows;
# webcam capture works without the GUI modules too).
opencv-python-headless>=5.0.0.93
