# QR generation — pure Python, no external dependencies of its own.
segno>=1.6.6

# Decoding the damaged images; numpy comes with it and is used directly
# for the pixel manipulation.
opencv-python-headless>=5.0.0.93
# numpy 2.5+ requires Python >=3.12; this project supports 3.11, so the floor
# stays on the 2.4 series (see the matching ignore rule in dependabot.yml).
numpy>=2.4.6
